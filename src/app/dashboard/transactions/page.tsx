import { createClient } from "@/utils/supabase/server";
export default async function Transactions(){const s=await createClient();const {data}=await s.from("credit_transactions").select("*").order("created_at",{ascending:false});return <section className="card overflow-hidden"><div className="border-b p-6"><h2 className="text-xl font-bold">Credit transactions</h2><p className="text-sm text-slate-500">A transparent record of every credit movement at ₹10 per credit.</p></div>{data?.length?<div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="p-4">Description</th><th className="p-4">Type</th><th className="p-4">Credits</th><th className="p-4">Amount</th><th className="p-4">Date</th></tr></thead><tbody>{data.map(t=><tr className="border-t" key={t.id}><td className="p-4 font-semibold">{t.description}</td><td className="p-4 capitalize">{t.transaction_type}</td><td className="p-4">{t.credits}</td><td className="p-4">₹{Math.abs(Number(t.amount)).toLocaleString("en-IN")}</td><td className="p-4 text-slate-500">{new Date(t.created_at).toLocaleString("en-IN")}</td></tr>)}</tbody></table></div>:<div className="p-16 text-center text-slate-500">No transactions yet.</div>}</section>}
