import { createClient } from "@/utils/supabase/server"; import { FileText } from "lucide-react";
export default async function Documents(){const s=await createClient();const {data}=await s.from("generated_documents").select("*").order("created_at",{ascending:false});return <section className="card overflow-hidden"><div className="border-b p-6"><h2 className="text-xl font-bold">Generated documents</h2><p className="text-sm text-slate-500">Your compliant demo document history.</p></div>{data?.length?<div className="divide-y">{data.map(d=><div key={d.id} className="flex items-center gap-4 p-5"><span className="rounded-xl bg-indigo-50 p-3 text-indigo-600"><FileText/></span><div className="flex-1"><p className="font-bold">{d.document_type}</p><p className="text-xs text-slate-500">{new Date(d.created_at).toLocaleString("en-IN")}</p></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">{d.status}</span></div>)}</div>:<Empty text="No generated documents yet."/>}</section>}
function Empty({text}:{text:string}){return <div className="p-16 text-center text-slate-500">{text}</div>}
