import { redirect } from "next/navigation"; import { AppShell } from "@/components/app-shell"; import { createClient } from "@/utils/supabase/server";
export default async function DashboardLayout({children}:{children:React.ReactNode}){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const [{data:profile},{data:admin},{data:blocked}]=await Promise.all([supabase.from("profiles").select("full_name").eq("user_id",user.id).maybeSingle(),supabase.from("admin_users").select("user_id").eq("user_id",user.id).maybeSingle(),supabase.from("blocked_users").select("user_id").eq("user_id",user.id).maybeSingle()]);if(blocked&&!admin)redirect("/account-blocked");return <AppShell name={profile?.full_name||user.user_metadata?.full_name||"User"} isAdmin={Boolean(admin)}>{children}</AppShell>}
